# Tasks: [Title]

> Generated by `/build` from the steps in `plan.md` — tracks build progress and enables resume. The executor checks a step off only after its verification passes; checked steps are skipped on re-run.

<!-- One line per plan step: - [ ] Step N: <title> — <ACTION> — <file> — Validation: <pass|fail|pending> -->
<!-- ACTION = the plan's CREATE/UPDATE/ADD/DELETE keyword where available -->
<!-- Validation = result of running the step's Validation Check: pending (not yet run), pass, or fail -->

- [ ] Step 1: ... — ACTION — `path/to/file` — Validation: pending
- [ ] Step 2: ... — ACTION — `path/to/file` — Validation: pending

<!-- Handoff footer — schema defined in templates/handoff-footer.md. MUTABLE: unlike the other four
     templates, agents/build-executor.md updates `verification` and `next_command` here alongside its
     per-step checkbox/Validation edits, so this stays current mid-build instead of going stale.
     Always below the one-line-per-step list, never inside it. -->
```loadout-handoff
schema: 1
artifact: tasks
produced_by: /build
work_item: .dev/<slug>
lane: standard | tdd | express
verification: pass | fail | none
next_command: /build
next_arguments: .dev/<slug>
```
